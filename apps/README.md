# K3s Apps

Kubernetes manifests are grouped by service.

Completed manifests currently committed:

- `infra/`
- `portfolios/`

Service manifests for FlashDeutsch, RAG Studio, finance-agent, n8n, Ollama, Homepage, and databases should be added when the sanitized YAMLs are available.
